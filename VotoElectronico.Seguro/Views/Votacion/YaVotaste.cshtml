@{
    ViewData["Title"] = "Voto ya registrado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="card shadow-lg border-0">
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <i class="bi bi-check2-circle text-success" style="font-size: 5rem;"></i>
                    </div>
                    <h2 class="fw-bold text-dark">¡Participación Registrada!</h2>
                    <p class="lead text-muted mt-3">
                        Usted ya ha ejercido su derecho al voto en este proceso electoral.
                    </p>
                    <div class="alert alert-info d-inline-block mt-2">
                        <i class="bi bi-info-circle-fill"></i> El sistema solo permite un registro por ciudadano.
                    </div>
                    <hr class="my-4">
                    <p>
                        Gracias por fortalecer la democracia digital de nuestro sistema.
                        Sus datos fueron sincronizados correctamente con la API el día de hoy.
                    </p>
                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-4">
                        <a href="/" class="btn btn-primary btn-lg px-4 gap-3">Ir al Inicio</a>
                        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post">
                            <button type="submit" class="btn btn-outline-secondary btn-lg px-4">Cerrar Sesión</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>v>